/**
 * Adapter registration barrel
 *
 * Importing this module registers all adapters with the central registry.
 * Add new adapter imports here when adding support for a new app.
 */

import './wezterm';
import './neovim';
import './sketchybar';
import './aerospace';
